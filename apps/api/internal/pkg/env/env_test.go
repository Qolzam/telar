package env

import (
	"testing"
)

// TestEnv_Basic ensures the package compiles and basic functionality works
func TestEnv_Basic(t *testing.T) {
	// Basic test to ensure the package compiles and can be imported
	t.Log("internal/pkg/env package test passed")
}

// TestEnv_Compilation ensures all functions can be called
func TestEnv_Compilation(t *testing.T) {
	// This test ensures that the env package compiles correctly
	t.Log("internal/pkg/env compilation test passed")
}
