// TODO: Re-enable when foundation plugin is migrated
// import { IFoundationService } from 'features/foundation/services/IFoundationService';

// Temporary type alias until plugin is migrated
// Using Record<string, never> prevents any properties from being added
export type IFoundationService = Record<string, never>;
